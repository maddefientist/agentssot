{
  "name": "agentssot",
  "version": "1.0.0",
  "description": "AgentSSOT unified memory -- MCP tools for recall, query, ingest, and administration",
  "author": "MadDefientist",
  "hooks": ["SessionStart", "SessionEnd"],
  "commands": ["hive"],
  "dependencies": {
    "uv": "required"
  },
  "settings": {
    "baseUrl": {
      "type": "string",
      "default": "http://localhost:8088",
      "description": "AgentSSOT API base URL"
    },
    "defaultNamespace": {
      "type": "string",
      "default": "default",
      "description": "Default namespace for recall/query/ingest"
    },
    "toolProfile": {
      "type": "string",
      "default": "core",
      "description": "Use core for six bounded memory tools; operator explicitly enables administrative and experimental tools"
    }
  }
}
