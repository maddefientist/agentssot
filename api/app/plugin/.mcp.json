{
  "mcpServers": {
    "agentssot": {
      "command": "uv",
      "args": ["run", "${CLAUDE_PLUGIN_ROOT}/mcp_server.py"],
      "env": {
        "HIVE_AGENT_JSON": "${HOME}/.claude/agentssot/local/agent.json",
        "HIVE_MCP_PROFILE": "core"
      }
    }
  }
}
