# MCP Plugin

The canonical hari-hive MCP plugin lives at `~/.claude/plugins/hari-hive/`.
This directory previously contained a stale copy that drifted from the canonical version (see review .chain/20260430-221455/review.md issue #4).
Do not re-add a copy here. The plugin is deployed via the Claude Code plugins system, not via the AgentSSOT container.